# Copy to .env and fill in. SentiSense reads these via os.getenv.

# Override the default data directory
# SENTISENSE_DATA_DIR=/path/to/data

# Override the default models directory
# SENTISENSE_MODELS_DIR=/path/to/models

# Enable DEBUG-level logging
# SENTISENSE_DEBUG=1
